#!/usr/bin/env bash

readonly OS_RELEASE="/etc/os-release"

[[ -r "$OS_RELEASE" ]] && . "$OS_RELEASE"

case "$ID" in
  arch) echo " " ;;
  debian) echo " " ;;
  nixos) echo " " ;;
  void) echo " " ;;
  *) echo " " ;;
esac
